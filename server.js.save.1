X
const express = require("express");

const app = express();

app.get("/", (req, res) => {
  res.send("Legal AI Server is Running");
});

app.listen(3000, () => {
  console.log("Server started on port 3000");
});ç
const express = require("express");

const app = express();

app.get("/", (req, res) => {
  res.send("Legal AI Server is Running");
});

app.listen(3000, () => {
  console.log("Server started on port 3000");
});

const express = require("express");

const app = express();

app.get("/", (req, res) => {
  res.send("Legal AI Server is Running");
});

app.listen(3000, () => {
  console.log("Server started on port 3000");
});
const express = require("express");

const app = express();

app.get("/", (req, res) => {
  res.send("Legal AI Server is Running");
});

app.listen(3000, () => {
  console.log("Server started on port 3000");
});o

const express = require("express");

const app = express();

app.get("/", (req, res) => {
  res.send("Legal AI Server is Running");
});

app.listen(3000, () => {
  console.log("Server started on port 3000");
})
